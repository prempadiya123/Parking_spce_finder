package com.example.gj3park;

import android.annotation.SuppressLint;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;

import androidx.appcompat.app.AppCompatActivity;

import com.example.gj3park.R;

public class sighup_profile extends AppCompatActivity {

   /* EditText firstNameEditText;
    EditText lastNameEditText;
    EditText addressEditText;
    EditText passwordEditText;
    Button createAccountButton;*/

    @SuppressLint("MissingInflatedId")
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main); // Replace with the actual layout file name

        // Initialize the UI elements
        /*firstNameEditText = findViewById(R.id.editTextTextPassword2);
        lastNameEditText = findViewById(R.id.editTextTextPassword3);
        addressEditText = findViewById(R.id.editTextTextPassword4);
        passwordEditText = findViewById(R.id.editTextTextPassword5);
        createAccountButton = findViewById(R.id.signin);

        createAccountButton.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
            }
        });*/
    }
}
